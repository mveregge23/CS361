@using System.Web.Script.Serialization
@model VirtualGarden.ViewModels.GardenFormViewModel
@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>Virtual Garden</h1>
    <p class="lead">Create and care for your own virutal garden.</p>
</div>

<div class="row">
    <div class="col-md-6">
        <h2>Create a Garden</h2>
        @using (Html.BeginForm("Create", "Gardens"))
        { 
            <p>All fields are required.</p>
            <div class="form-group">
                @Html.LabelFor(m => m.Name)
                @Html.TextBoxFor(m => m.Name, new { @class = "form-control", autofocus = "autofocus" })
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.Location)
                @Html.DropDownListFor(m => m.Location, new SelectList(Model.Locations, "Id", "Name"), "", new {@class = "form-control" })
            </div>
            <button type="submit" class="btn btn-primary btn-lg">Create</button>
        }
    </div>
    <div class="col-md-6">
        <h2>Open a Garden</h2>
        <p>Enter the name of the garden to open it.</p>
    </div>
</div>